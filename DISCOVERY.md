# Component Inventory (Repo A)

| Path | Purpose | Inputs | Outputs | Key Exports / Interfaces | External Dependencies | Env Vars |
| --- | --- | --- | --- | --- | --- | --- |
| packages/core/src/agent/index.ts | Core `SolanaAgentKit` orchestrating connection, wallet, plugin registry. | Wallet implementing `BaseWallet`, RPC URL string, config object. | Initialized agent with merged plugin methods/actions. | `SolanaAgentKit` class. | `@solana/web3.js` connection client. | Reads config fields (e.g., HELIUS_API_KEY) indirectly via plugins/utilities. |
| packages/core/src/types/wallet.ts | Wallet abstraction and transaction dispatcher. | Agent instance, transactions or instruction arrays, optional fee tier/keypairs. | Signed tx(s), signatures, or execution errors. | `BaseWallet`, `signOrSendTX`. | `@solana/web3.js` for transactions; internal `send_tx`. | Uses agent.config flags (`signOnly`). |
| packages/core/src/utils/send_tx.ts | Priority-fee transaction builder/executor. | Agent, instructions, optional keypairs/fee tier. | Transaction signature string. | `getComputeBudgetInstructions`, `sendTx`, `feeTiers`. | `@solana/web3.js`, `bs58`, `ComputeBudgetProgram`. | Checks `agent.config.HELIUS_API_KEY` for Helius RPC fee estimate. |
| packages/core/src/utils/actionExecutor.ts | Action validation/execution helper. | Action definition, agent, input record. | Structured success/error payloads. | `executeAction`, `getActionExamples`. | `zod` via action schema. | None. |
| packages/core/src/utils/keypairWallet.ts | Local keypair-backed wallet impl. | `Keypair`, RPC URL. | Signing/sending via Solana RPC. | `KeypairWallet`, `isVersionedTransaction`. | `@solana/web3.js`, `tweetnacl`. | RPC URL for `Connection`. |
| packages/core/src/utils/getMintInfo.ts | Token mint metadata fetcher. | Connection, mint address string, optional commitment. | SPL token mint info object. | `getMintInfo`. | `@solana/spl-token`, `@solana/web3.js`. | None. |
| packages/core/src/constants/index.ts | Default token/program constants. | None (static). | Default config values, well-known mint addresses. | `TOKENS`, `DEFAULT_OPTIONS`. | `@solana/web3.js`. | None. |
| packages/core/src/types/index.ts | Shared types for plugins/actions/config. | TypeScript definitions. | Interfaces for `Plugin`, `Config`, `Action`, etc. | Re-exported type definitions. | Type-only. | Config references (e.g., API keys). |
| packages/core/src/langchain/index.ts | LangChain tool adapter for actions. | Agent, action list. | Array of LangChain tool instances. | `createLangchainTools`. | `@langchain/core/tools`. | None. |
| packages/core/src/openai/index.ts & utils.ts | OpenAI agent tool adapter & schema translator. | Agent, actions, Zod schemas. | Array of OpenAI function tools. | `createOpenAITools`, `zodToOpenAITool`. | `@openai/agents`, `zod`. | None. |
| packages/core/src/vercel-ai/index.ts | Vercel AI SDK tool adapter. | Agent, actions. | Record of AI tools keyed by index. | `createSolanaTools`. | `ai` SDK. | None. |
| packages/adapter-mcp/src/index.ts | Model Context Protocol bridge. | Action map, agent, MCP metadata. | MCP server exposing tools/prompts. | `createMcpServer`, `startMcpServer`, `zodToMCPShape`. | `@modelcontextprotocol/sdk`, `zod`. | None. |
| packages/plugin-token/src/index.ts | Token-centric plugin aggregator. | Agent provided at runtime. | Plugin with token actions/methods. | Default export `TokenPlugin`. | Multiple Solana/3rd-party SDKs via submodules. | Uses agent.config keys (JUPITER, FLEXLEND, etc.) indirectly. |
| packages/plugin-token/src/jupiter/tools/trade.ts | Jupiter swap executor. | Agent, output mint, amount, optional input mint/slippage. | Transaction signature or signed tx. | `trade`. | Fetch to Jupiter API, `@solana/web3.js`. | Reads `agent.config` for referral/priority levels. |
| packages/plugin-defi/src/orca/tools/orca_create_clmm.ts | Orca CLMM pool creation executor. | Agent, mint keys, initial price, fee tier. | JSON string containing tx id & pool address. | `orcaCreateCLMM`. | `@orca-so/whirlpools-sdk`, `decimal.js`, `@solana/web3.js`. | Checks agent connection endpoint for network selection. |
| packages/plugin-misc/src/switchboard/tools/simulate_feed.ts | Switchboard oracle simulator. | Feed pubkey string, optional Crossbar URL. | Simulation result string. | `simulate_switchboard_feed`. | `@switchboard-xyz/common`. | Defaults to `SWITCHBOARD_DEFAULT_CROSSBAR`. |
| packages/plugin-token/src/rugcheck/tools/rugcheck.ts | Token risk heuristic fetcher. | Token mint string. | Parsed rug-check report JSON. | `fetchTokenReportSummary`, `fetchTokenDetailedReport`. | HTTP fetch to RugCheck API. | None. |
| examples/social/tg-bot-starter/basic-tg-bot/src/app/api/bot/route.ts | Basic Telegram webhook handler wiring LLM+agent. | Telegram webhook request, env secrets, user text. | Replies via Telegram API. | Next.js `POST` handler, `initializeAgent`. | `grammy`, `@langchain` stack, `solana-agent-kit`. | TELEGRAM_BOT_TOKEN, SOLANA_PRIVATE_KEY, RPC_URL. |
| examples/social/tg-bot-starter/group-tg-bot/src/app/api/bot/route.ts | Group Telegram handler with per-user wallets/state. | Telegram updates, Firebase/Postgres, env config. | Telegram replies & wallet provisioning. | Webhook POST, `initializeAgent`, `getOrCreateUserKeyPair`. | `grammy`, Firebase, LangChain, Postgres, Solana Agent kit. | TELEGRAM_BOT_TOKEN, RPC_URL, Firebase config, POSTGRES_LINK. |
